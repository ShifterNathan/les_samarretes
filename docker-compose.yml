version: '3.1'

services:
  mongodb:
    container_name: mongodb
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGODB_DATABASE: les_samarretes
    volumes:
      - ./mongo:/data/db
